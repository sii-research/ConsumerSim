// Optional static-hosting override.
// For the recommended consumersim-web deployment, this file is not needed because
// the website reads /api/site-data from the local web bridge.
window.CONSUMERSIM_SITE_DATA_URL = "https://your-private-consumersim.example.com/site-data";
