// Optional static-hosting override. Copy to site-config.js for local static
// testing, or set CONSUMERSIM_SITE_DATA_URL in GitHub repository variables so
// the Pages workflow generates site/site-config.js during deployment.
// For the consumersim-web deployment, this file is not needed because the
// website reads /api/site-data from the local web bridge.
window.CONSUMERSIM_SITE_DATA_URL = "https://your-private-consumersim.example.com/site-data";
